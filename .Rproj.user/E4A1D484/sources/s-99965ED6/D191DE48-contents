sub.names <- unlist(strsplit(original.names, split = " "))

# Concatenates 3 random sub-names from sub.names vector
random_name <- function() {
  return(paste(sample(sub.names, size = 3), collapse = " "))
}